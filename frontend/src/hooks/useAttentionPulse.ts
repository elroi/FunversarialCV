/**
 * Reusable attention pulse helpers (CSS: `.attention-pulse` in globals.css).
 * Implementation lives in `lib/attentionPulse` for unit testing without React.
 */
export {
  ATTENTION_PULSE_CLASS,
  prefersReducedMotion,
  restartAttentionPulse,
} from "../lib/attentionPulse";
